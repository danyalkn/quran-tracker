// Build marker. Bumped on every deploy so running clients can detect that a
// newer build is live and reload themselves (see UpdateChecker + /api/version).
// iOS standalone PWAs otherwise keep running the in-memory bundle indefinitely.
export const BUILD = "2026-06-09-c";
